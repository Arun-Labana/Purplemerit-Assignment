apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: collaborative-workspace
  labels:
    app: collaborative-workspace
data:
  NODE_ENV: "production"
  PORT: "3000"
  API_VERSION: "v1"
  JWT_EXPIRES_IN: "15m"
  JWT_REFRESH_EXPIRES_IN: "7d"
  CORS_ORIGIN: "https://your-frontend-domain.com"
  LOG_LEVEL: "info"
  RATE_LIMIT_WINDOW_MS: "60000"
  RATE_LIMIT_MAX_REQUESTS: "100"
  # Database URLs
  # Option 1: Use in-cluster PostgreSQL and Redis (deployed via k8s/postgres-deployment.yaml and k8s/redis-deployment.yaml)
  DATABASE_URL: "postgresql://postgres:${POSTGRES_PASSWORD}@postgres-service:5432/collaborative_workspace"
  REDIS_URL: "redis://redis-service:6379"
  # Option 2: Use external databases (AWS RDS, ElastiCache, etc.) - uncomment and update:
  # DATABASE_URL: "postgresql://user:password@your-rds-endpoint:5432/collaborative_workspace"
  # REDIS_URL: "redis://your-elasticache-endpoint:6379"
  
  # External services (already configured)
  MONGODB_URI: "mongodb+srv://labanaarun0_db_user:Password1@cluster0.w7emiil.mongodb.net/collaborative_workspace?retryWrites=true&w=majority"
  RABBITMQ_URL: "amqps://fcsjowty:vtFpaBA2HlHoldtOA3encf__AYHDH_Ma@leopard.lmq.cloudamqp.com/fcsjowty"

